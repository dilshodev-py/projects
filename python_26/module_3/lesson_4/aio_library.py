# import asyncio
# import json
#
# import aiofiles

# async def open_file(data , filename):
#     async with aiofiles.open(filename, mode='r') as f:
    # json.dump(data , f , indent=3)
    # data = json.dumps(data , indent=3)
    # await f.write(data)
    # data = await f.read()
    # return json.loads(data)

# asyncio.run(open_file([1,2,3] , 'users.json'))
