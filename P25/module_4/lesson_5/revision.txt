commit:
    INSERT
    UPDATE
    DELETE

sequence:
    CREATE SEQUENCE S_NAME START 10 MINVALUE 1 MAXVALUE 100;

ID SERIAL

constraint:
    PRIMARY KEY
    FOREIGN KEY
    UNIQUE
    DEFAULT
    NOT NULL
    CHECK

count:
    SELECT NAME , COUNT(*) FROM BOOKS;

PRIMARY KEY - NOT NULL , UNIQUE , INDEX
FOREIGN KEY - EXISTS CHECK
UNIQUE - YAGONALIK
DEFAULT -
NOT NULL -
CHECK -



